{
  "name": "civil-affairs",
  "version": "1.0.0",
  "description": "Township civil administration and resident service portal",
  "main": "server.js",
  "scripts": {
    "start": "node -r ./qr-management-server.js -r ./resident-services-server.js -r ./admin-applications-loader.js session-store-patch.js",
    "dev": "node -r ./qr-management-server.js -r ./resident-services-server.js -r ./admin-applications-loader.js --watch session-store-patch.js",
    "test": "node tests/api-test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "better-sqlite3-session-store": "^0.1.0",
    "express": "^4.21.2",
    "express-session": "^1.19.0",
    "helmet": "^8.0.0"
  }
}
