(()=>{const p=new URLSearchParams(location.search),type=(p.get('type')||'').toLowerCase();if(!type||!p.get('code'))return;const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));const data={type,title:p.get('title')||'',location:p.get('location')||'',category:p.get('category')||'',description:p.get('description')||'',quarterNumber:p.get('quarterNumber')||'',shopNumber:p.get('shopNumber')||'',dustbinId:p.get('dustbinId')||'',gateName:p.get('gateName')||'',gateType:p.get('gateType')||'',openingTime:p.get('openingTime')||'',closingTime:p.get('closingTime')||'',gatekeeperName:p.get('gatekeeperName')||'',details:p.get('details')||'',rules:(p.get('rules')||'').split('|').map(x=>x.trim()).filter(Boolean)};const complaint=type.includes('quarter')?'Quarter maintenance':type.includes('shop')?'Shop operation':type.includes('dustbin')||type.includes('garbage')||type.includes('bin')?'Garbage / sanitation':'Public issue';const form=document.getElementById('report-form');if(!form)return;const fill=(name,value)=>{if(!value)return;const x=form.querySelector(`[name="${name}"]`);if(x)x.value=value};fill('type',complaint);fill('category',data.category||(type.includes('quarter')?'Maintenance request':type.includes('shop')?'Shop operation':type.includes('dustbin')?'Garbage / sanitation':'Other'));fill('location',data.location||data.quarterNumber||data.shopNumber||data.dustbinId||data.gateName||data.title);if(type.includes('gate')){const wrap=document.createElement('div');wrap.className='qr-entry-notice';wrap.innerHTML=`<div><b>${esc(data.gateName||data.title||'Township gate')}</b><p>${esc(data.gateType||'Gate')} · ${esc(data.openingTime||'—')} – ${esc(data.closingTime||'—')}</p>${data.gatekeeperName?`<p>Gatekeeper: ${esc(data.gatekeeperName)}</p>`:''}${data.rules.length?`<ul>${data.rules.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`:''}</div><button type="button" class="button" id="qr-entry-report">Report an issue</button>`;document.querySelector('.resident-main')?.prepend(wrap);wrap.querySelector('#qr-entry-report').onclick=()=>{document.getElementById('report-modal')?.classList.add('show');wrap.remove()};}else{document.getElementById('report-modal')?.classList.add('show');}history.replaceState({},'',location.pathname);})();
